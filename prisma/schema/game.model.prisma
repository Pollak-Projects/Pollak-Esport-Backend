model Game {
    id                  String         @id @default(uuid())
    name                String
    createdAt           DateTime       @default(now())
    updatedAt           DateTime       @updatedAt
    playerCount         Int
    playerPerTeam       Int
    requiredPlayerCount Int
    Round               Round[]
    roundsOnGame        roundsOnGame[]
}
